aw.position.set(0,-.18,.04);headGroup.add(jaw);
  for(const x of [-.14,.14]){const eye=new THREE.Mesh(new THREE.SphereGeometry(.045,12,8),std(0xf8f7f2,.5,0));eye.scale.set(1.2,.75,.45);eye.position.set(x,.04,-.34);headGroup.add(eye);const pupil=new THREE.Mesh(new THREE.SphereGeometry(.018,10,6),std(0x25323b,.45,0));pupil.position.set(x,.04,-.375);headGroup.add(pupil);const brow=new THREE.Mesh(new THREE.BoxGeometry(.15,.025,.025),mats.hair);brow.position.set(x,.14,-.35);brow.rotation.z=x<0?-.08:.08;headGroup.add(brow);}
  const nose=new THREE.Mesh(new THREE.SphereGeometry(.055,10,8),mats.skin);nose.scale.set(.75,1.05,.8);nose.position.set(0,-.03,-.37);headGroup.add(nose);
  const mouth=new THREE.Mesh(new THREE.BoxGeometry(.17,.022,.018),std(0x8e5151,.7,0));mouth.position.set(0,-.17,-.35);headGroup.add(mouth);
  const hair=new THREE.Group();headGroup.add(hair);for(const [x,y,z,s] of [[-.22,.24,0,.85],[0,.32,.01,1],[.22,.24,0,.85],[-.31,.12,.02,.64],[.31,.12,.02,.64]]){const h=new THREE.Mesh(new THREE.SphereGeometry(.2,14,10),mats.hair);h.scale.set(s,.55,.76);h.position.set(x,y,z);hair.add(h);}
  if(['erin','shannan','kelly'].includes(charId)){for(const x of [-.31,.31]){const h=new THREE.Mesh(new THREE.SphereGeometry(.18,14,10),mats.hair);h.scale.set(.65,1.75,.68);h.position.set(x,-.12,.02);hair.add(h);}}
  // arms
  function makeArm(side){const s=side<0?-1:1,shoulder=new THREE.Group();shoulder.position.set(.55*s,1.92,0);visual.add(shoulder);const cap=new THREE.Mesh(new THREE.SphereGeometry(.17,12,9),mats.shirt);cap.position.y=0;shoulder.add(cap);const upper=new THREE.Mesh(new THREE.CylinderGeometry(.12,.145,.42,12),mats.shirt);upper.position.y=-.22;shoulder.add(upper);const elbow=new THREE.Group();elbow.position.y=-.43;shoulder.add(elbow);const ej=new THREE.Mesh(new THREE.SphereGeometry(.12,10,8),mats.skin);elbow.add(ej);const lower=new THREE.Mesh(new THREE.CylinderGeometry(.095,.115,.42,12),mats.skin);lower.position.y=-.22;elbow.add(lower);const hand=new THREE.Mesh(new THREE.SphereGeometry(.125,12,8),mats.skin);hand.scale.set(.85,.95,.8);hand.position.set(0,-.46,-.02);elbow.add(hand);return{shoulder,elbow,hand};}
  const leftArm=makeArm(-1),rightArm=makeArm(1);
  // legs
  function makeLeg(side){const s=side<0?-1:1,hip=new THREE.Group();hip.position.set(.23*s,.92,0);visual.add(hip);const thigh=new THREE.Mesh(new THREE.CylinderGeometry(.145,.17,.52,12),mats.pants);thigh.position.y=-.27;hip.add(thigh);const knee=new THREE.Group();knee.position.y=-.54;hip.add(knee);const calf=new THREE.Mesh(new THREE.CylinderGeometry(.11,.14,.49,12),mats.pants);calf.position.y=-.25;knee.add(calf);const shoe=new THREE.Mesh(new THREE.BoxGeometry(.32,.18,.5),mats.shoe);shoe.p